- hosts: localhost
  tasks:
    - name: install packages
      become: true
      become_user: root
      apt:
        state: present
        name:
         - yamllint
         - ansible-lint
         - htop
         - zsh
         - python3.8
         - golang
